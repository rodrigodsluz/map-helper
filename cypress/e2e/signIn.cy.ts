describe('Test Sign in page', () => {
  it('should navigate to the homepage', () => {
    cy.visit('http://localhost:3000/');

    //cy.findByText('Faça seu pedido agora sem enrolação').should('exist');
  });
});
