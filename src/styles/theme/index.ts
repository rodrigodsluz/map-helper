import animations from './animations';
import colors from './darkThemeColors';
import fonts from './fonts';
import mediaQueries from './mediaQueries';

export const theme = {
  colors,
  fonts,
  mediaQueries,
  animations,
} as const;
